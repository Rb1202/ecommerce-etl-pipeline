import os
from dotenv import load_dotenv

load_dotenv()

DB_CONFIG = {
    "host": "localhost",
    "port": "5432",
    "database": "e-commerce_db",
    "user": "postgres",
    "password": "rushi123"
}